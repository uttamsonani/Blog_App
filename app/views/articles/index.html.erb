<h1 class="text-center pb-2 mb-2">ARTICLES</h1>

<p>Our blog has <%= Article.public_count %> articles and counting!</p>

<ol>
  <% @articles.each do |article| %>
    <% unless article.archived? %>
      <li class="text-uppercase">
        <%= link_to article.title, article, class: "text-decoration-none text-black" %>
      </li>
    <% end %>
  <% end %>
</ol>

<div class="d-grid">
  <%= link_to "New Article", new_article_path, class: "btn classic btn-sm btn-block" %>
</div>  

<%# TIME FUNCTION %>
<%# <div class="time-container">
   <%= render partial: "date" %>
<%# </div>  %>